concat(
    substring(
        items('For_each')?['SharePointLink'],
        lastIndexOf(items('For_each')?['SharePointLink'], '/sites/TradingIT') + length('/sites/TradingIT'),
        sub(
            lastIndexOf(items('For_each')?['SharePointLink'], '/'),
            lastIndexOf(items('For_each')?['SharePointLink'], '/sites/TradingIT') + length('/sites/TradingIT')
        )
    ),
    '/',
    encodeUriComponent(
        last(split(items('For_each')?['SharePointLink'], '/'))
    )
)
