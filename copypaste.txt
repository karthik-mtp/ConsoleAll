 Message Lock Timeout
Each message has a lock duration (default: 30 seconds).

If your WebJob doesn't complete processing before the lock expires, the message is re-delivered.

That causes the WebJob to trigger again — up to MaxDeliveryCount times.
